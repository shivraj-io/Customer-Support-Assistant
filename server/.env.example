# Set one provider key for live classification.
OPENAI_API_KEY=

# MongoDB connection string.
MONGO_URI=

# Optional overrides. Defaults are suitable for the respective APIs.
AI_PROVIDER=
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_MODEL=claude-3-5-haiku-latest
